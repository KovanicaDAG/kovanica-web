import { t as ATOM } from "./types-oiN7huTa.js";
//#region src/lib/ledger/format.ts
function fmtKvnc(atoms) {
	return `${(atoms / 1e8).toFixed(8).replace(/\.?0+$/, "") || "0"} KVNC`;
}
function parseKvnc(raw) {
	const n = Number(String(raw).trim().replace(",", "."));
	if (!Number.isFinite(n) || n <= 0) return null;
	return Math.round(n * ATOM);
}
//#endregion
export { parseKvnc as n, fmtKvnc as t };
