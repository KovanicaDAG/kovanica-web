import "./types-oiN7huTa.js";
import { n as apiPostJson, t as api } from "./client-D8mA7dud.js";
import { x as Button } from "./router-bgBdHzxi.js";
import { t as Shell } from "./shell-BLfuH2qc.js";
import { n as parseKvnc, t as fmtKvnc } from "./format-Bvs5ygaA.js";
import { useState } from "react";
import { Fragment, jsx, jsxs } from "react/jsx-runtime";
import { Clock, Unlock, Vault } from "lucide-react";
import { toast } from "sonner";
//#region src/lib/protocol/vaults.ts
async function createVault(params) {
	const beneficiary = params.beneficiaryPubkeyHex.trim().toLowerCase();
	if (!/^[0-9a-f]{64}$/.test(beneficiary)) throw new Error("Beneficiary pubkey must be 64-char hex");
	if (params.amountAtoms <= 0) throw new Error("Amount must be positive");
	if (params.unlockAt < 1) throw new Error("Unlock height/blocks must be ≥ 1");
	const res = await apiPostJson("/api/vault/create", {
		beneficiary_pubkey_hex: beneficiary,
		amount_atoms: params.amountAtoms,
		lock_type: params.lockType,
		unlock_at: params.unlockAt
	});
	return {
		address: res.address,
		redeemScriptHex: res.redeem_script_hex,
		beneficiaryPubkeyHex: res.beneficiary_pubkey_hex,
		lockType: res.lock_type,
		unlockAt: res.unlock_at,
		amountAtoms: res.amount_atoms
	};
}
async function getVaultStatus(addressOrOutpoint) {
	const q = addressOrOutpoint.includes(":") ? `outpoint=${encodeURIComponent(addressOrOutpoint.trim())}` : `address=${encodeURIComponent(addressOrOutpoint.trim())}`;
	return api(`/api/vault/status?${q}`);
}
async function claimVault(params) {
	return { txIdHex: (await apiPostJson("/api/vault/claim", {
		outpoint: {
			tx: params.outpointTx.trim(),
			index: params.outpointIndex
		},
		destination: params.destination.trim(),
		beneficiary_secret_hex: params.beneficiarySecretHex?.trim().toLowerCase() || void 0
	})).tx_id_hex };
}
function parseOutpoint(raw) {
	const [tx, idx] = raw.trim().split(":");
	if (!tx || idx === void 0 || Number.isNaN(Number(idx))) throw new Error("Outpoint must be txid:index");
	return {
		tx,
		index: Number(idx)
	};
}
//#endregion
//#region src/components/protocol/vaults-view.tsx
/** KVP-105 / RFC-005 — Vaults with live API calls. */
function VaultsView() {
	const [tab, setTab] = useState("create");
	const [beneficiary, setBeneficiary] = useState("");
	const [amount, setAmount] = useState("1000000");
	const [lockType, setLockType] = useState("cltv");
	const [unlockAt, setUnlockAt] = useState("2000000");
	const [created, setCreated] = useState(null);
	const [query, setQuery] = useState("");
	const [status, setStatus] = useState(null);
	const [claimOut, setClaimOut] = useState("");
	const [claimDest, setClaimDest] = useState("");
	const [busy, setBusy] = useState(false);
	const [lastTx, setLastTx] = useState(null);
	async function onCreate() {
		const atoms = parseKvnc(amount);
		if (atoms === null || atoms <= 0) {
			toast.error("Enter a positive amount");
			return;
		}
		const at = Number(unlockAt);
		if (!Number.isFinite(at) || at < 1) {
			toast.error("Unlock must be ≥ 1");
			return;
		}
		setBusy(true);
		try {
			const res = await createVault({
				beneficiaryPubkeyHex: beneficiary,
				amountAtoms: atoms,
				lockType,
				unlockAt: at
			});
			setCreated(res);
			toast.success("Vault address created");
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "Create failed");
		} finally {
			setBusy(false);
		}
	}
	async function onStatus() {
		if (!query.trim()) {
			toast.error("Enter address or outpoint");
			return;
		}
		setBusy(true);
		try {
			const res = await getVaultStatus(query.trim());
			setStatus(res);
			toast.success(res.unlocked ? "Unlocked" : `${res.blocksRemaining} blocks remaining`);
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "Status failed");
		} finally {
			setBusy(false);
		}
	}
	async function onClaim() {
		setBusy(true);
		try {
			const { tx, index } = parseOutpoint(claimOut);
			const res = await claimVault({
				outpointTx: tx,
				outpointIndex: index,
				destination: claimDest.trim()
			});
			setLastTx(res.txIdHex);
			toast.success(`Claimed · ${res.txIdHex.slice(0, 16)}…`);
		} catch (e) {
			toast.error(e instanceof Error ? e.message : "Claim failed");
		} finally {
			setBusy(false);
		}
	}
	return /* @__PURE__ */ jsxs("main", {
		className: "mx-auto w-full max-w-2xl flex-1 px-4 pb-12 pt-8 md:px-8",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "font-mono text-[11px] tracking-brand text-blue uppercase",
				children: "KVP-105 · RFC-005"
			}),
			/* @__PURE__ */ jsx("h1", {
				className: "mt-1 font-display text-3xl tracking-tight text-fg italic md:text-4xl",
				children: "Vaults"
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-2 max-w-lg text-sm leading-relaxed text-muted",
				children: "CLTV / CSV time-lock vaults and treasury vesting."
			}),
			/* @__PURE__ */ jsx("div", {
				className: "mt-8 inline-flex rounded-md bg-surface-2 p-0.5",
				children: [
					{
						id: "create",
						label: "Create",
						icon: Vault
					},
					{
						id: "status",
						label: "Status",
						icon: Clock
					},
					{
						id: "claim",
						label: "Claim",
						icon: Unlock
					}
				].map(({ id, label, icon: Icon }) => /* @__PURE__ */ jsxs("button", {
					type: "button",
					onClick: () => setTab(id),
					className: tab === id ? "inline-flex h-9 items-center gap-1.5 rounded-sm bg-bg px-3 text-sm font-medium text-fg" : "inline-flex h-9 items-center gap-1.5 rounded-sm px-3 text-sm font-medium text-muted hover:text-fg",
					children: [/* @__PURE__ */ jsx(Icon, { className: "size-3.5" }), label]
				}, id))
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "mt-6 rounded-xl border border-border bg-surface p-5 space-y-4",
				children: [
					tab === "create" && /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx("input", {
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 font-mono text-sm",
							placeholder: "Beneficiary pubkey",
							value: beneficiary,
							onChange: (e) => setBeneficiary(e.target.value)
						}),
						/* @__PURE__ */ jsx("input", {
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 font-mono text-sm",
							value: amount,
							onChange: (e) => setAmount(e.target.value),
							placeholder: "Amount KVNC"
						}),
						/* @__PURE__ */ jsxs("select", {
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 text-sm",
							value: lockType,
							onChange: (e) => setLockType(e.target.value),
							children: [/* @__PURE__ */ jsx("option", {
								value: "cltv",
								children: "CLTV"
							}), /* @__PURE__ */ jsx("option", {
								value: "csv",
								children: "CSV"
							})]
						}),
						/* @__PURE__ */ jsx("input", {
							type: "number",
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 font-mono text-sm",
							value: unlockAt,
							onChange: (e) => setUnlockAt(e.target.value)
						}),
						/* @__PURE__ */ jsx(Button, {
							disabled: busy,
							onClick: () => void onCreate(),
							children: busy ? "…" : "Create vault"
						}),
						created && /* @__PURE__ */ jsxs("p", {
							className: "break-all font-mono text-xs",
							children: [
								created.address,
								" · ",
								fmtKvnc(created.amountAtoms)
							]
						}),
						/* @__PURE__ */ jsxs("p", {
							className: "text-xs text-subtle",
							children: [
								"POST /api/vault/create · 1 KVNC = ",
								1e8.toLocaleString(),
								" atoms"
							]
						})
					] }),
					tab === "status" && /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx("input", {
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 font-mono text-sm",
							placeholder: "address or txid:index",
							value: query,
							onChange: (e) => setQuery(e.target.value)
						}),
						/* @__PURE__ */ jsx(Button, {
							variant: "outline",
							disabled: busy,
							onClick: () => void onStatus(),
							children: busy ? "…" : "Check status"
						}),
						status && /* @__PURE__ */ jsxs("p", {
							className: "font-mono text-xs",
							children: [
								status.unlocked ? "UNLOCKED" : "LOCKED",
								" · ",
								fmtKvnc(status.amountAtoms),
								" · remaining ",
								status.blocksRemaining
							]
						})
					] }),
					tab === "claim" && /* @__PURE__ */ jsxs(Fragment, { children: [
						/* @__PURE__ */ jsx("input", {
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 font-mono text-sm",
							placeholder: "txid:index",
							value: claimOut,
							onChange: (e) => setClaimOut(e.target.value)
						}),
						/* @__PURE__ */ jsx("input", {
							className: "w-full rounded-md border border-border bg-bg px-3 py-2 font-mono text-sm",
							placeholder: "Destination",
							value: claimDest,
							onChange: (e) => setClaimDest(e.target.value)
						}),
						/* @__PURE__ */ jsx(Button, {
							disabled: busy,
							onClick: () => void onClaim(),
							children: busy ? "…" : "Claim"
						}),
						lastTx && /* @__PURE__ */ jsx("p", {
							className: "font-mono text-xs",
							children: lastTx
						})
					] })
				]
			})
		]
	});
}
//#endregion
//#region src/routes/vaults.tsx?tsr-split=component
function VaultsPage() {
	return /* @__PURE__ */ jsx(Shell, { children: /* @__PURE__ */ jsx(VaultsView, {}) });
}
//#endregion
export { VaultsPage as component };
